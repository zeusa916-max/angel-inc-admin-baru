import{createClient}from'@/lib/supabase/server';import ProductForm from'@/components/product-form';export default async function New(){const{data:categories}=await(await createClient()).from('categories').select('id,name').eq('is_active',true).order('name');return <><h1 className="font-display text-4xl">Tambah Produk</h1><div className="mt-7"><ProductForm categories={categories||[]}/></div></>}

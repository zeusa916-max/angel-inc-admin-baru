import{notFound}from'next/navigation';import{createClient}from'@/lib/supabase/server';import ProductForm from'@/components/product-form';export default async function Edit({params}:{params:Promise<{id:string}>}){const{id}=await params;const db=await createClient();const[{data:p},{data:c}]=await Promise.all([db.from('products').select('*,product_images(*)').eq('id',id).single(),db.from('categories').select('id,name').eq('is_active',true)]);if(!p)notFound();return <><h1 className="font-display text-4xl">Edit Produk</h1><div className="mt-7"><ProductForm categories={c||[]} product={p}/></div></>}

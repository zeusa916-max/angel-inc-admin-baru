import Link from'next/link';import{createClient}from'@/lib/supabase/server';import{idr,date}from'@/lib/utils';export default async function Orders(){const{data}=await(await createClient()).from('orders').select('id,total,status,created_at,customers(name,email)').order('created_at',{ascending:false});return <div><h1 className="font-display text-4xl">Pesanan</h1><div className="mt-7 overflow-x-auto rounded-2xl border bg-white"><table className="w-full min-w-[700px] text-left text-sm"><thead className="border-b text-xs text-neutral-400"><tr><th className="p-4">ID</th><th>Pelanggan</th><th>Total</th><th>Status</th><th>Tanggal</th><th></th></tr></thead><tbody>{(data||[]).map(o=><tr key={o.id} className="border-b"><td className="p-4 font-mono text-xs">{o.id.slice(0,8)}…</td><td>{o.customers?.name||'—'}</td><td>{idr(o.total)}</td><td>{o.status}</td><td>{date(o.created_at)}</td><td><Link href={`/admin/orders/${o.id}`} className="rounded-lg border px-3 py-2 text-xs">Detail</Link></td></tr>)}</tbody></table></div></div>}

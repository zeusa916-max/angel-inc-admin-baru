import{notFound}from'next/navigation';import{createClient}from'@/lib/supabase/server';export default async function Customer({params}:{params:Promise<{id:string}>}){const{id}=await params;const{data:c}=await(await createClient()).from('customers').select('*').eq('id',id).single();if(!c)notFound();return <div><h1 className="font-display text-4xl">{c.name}</h1><div className="mt-7 rounded-2xl border bg-white p-6 text-sm leading-7">Email: {c.email}<br/>Telepon: {c.phone||'—'}<br/>Bergabung: {new Date(c.created_at).toLocaleDateString('id-ID')}</div></div>}
